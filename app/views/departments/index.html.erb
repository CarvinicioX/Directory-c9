<p id="notice"><%= notice %></p>

<h1>Departamentos</h1>

<table>
  <thead>
    <tr>
      <th>Código</th>
      <th>Departamento</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.num_depto %></td>
        <td><%= department.name %></td>
        <td><%= link_to 'Mostrar', department %></td>
        <td><%= link_to 'Municipio', municipalities_path(:department => department) %></td>
        <td><%= link_to 'Modificar', edit_department_path(department) %></td>
        <td><%= link_to 'Eliminar', department, method: :delete, data: { confirm: 'Esta Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Agregar Departamento', new_department_path %>
