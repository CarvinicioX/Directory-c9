<%= form_for(mer) do |f| %>
  <% if mer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mer.errors.count, "error") %> prohibited this mer from being saved:</h2>

      <ul>
      <% mer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :polling_station_id %>
    <%= f.text_field :polling_station_id %>
  </div>

  <div class="field">
    <%= f.label :num_mer %>
    <%= f.text_field :num_mer %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
